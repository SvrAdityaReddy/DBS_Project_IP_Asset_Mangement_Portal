<%= form_for(own) do |f| %>
  <% if own.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(own.errors.count, "error") %> prohibited this own from being saved:</h2>

      <ul>
      <% own.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :precentage %>
    <%= f.text_field :precentage %>
  </div>

  <div class="field">
    <%= f.label :email %>
    <%= f.text_field :email %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
