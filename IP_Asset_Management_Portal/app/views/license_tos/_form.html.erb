<%= form_for(license_to) do |f| %>
  <% if license_to.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(license_to.errors.count, "error") %> prohibited this license_to from being saved:</h2>

      <ul>
      <% license_to.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :uid %>
    <%= f.number_field :uid %>
  </div>

  <div class="field">
    <%= f.label :license_agreement %>
  <%= f.file_field :licenceagreement , required: true%>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
